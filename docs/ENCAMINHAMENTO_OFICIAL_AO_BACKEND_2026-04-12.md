# Encaminhamento Oficial Ao Backend

Data de referencia: `2026-04-12`

## Contexto

Este encaminhamento acompanha o documento:

- `DECISOES_PENDENTES_DO_ECOSSISTEMA_2026-04-12.md`

O material foi consolidado a partir da leitura compartilhada entre:

- `App Morador`
- `Guarita`
- `Portaria Web`

## Leitura Atual

Os tres fronts convergem na direcao geral do ecossistema.

Hoje, o principal ponto pendente ja nao e tela ou fluxo local.

O principal ponto pendente e definicao oficial de contrato do `Backend`.

## Pedido Ao Backend

Solicitamos retorno objetivo, preferencialmente item a item, marcando para cada decisao:

- `contrato vigente`
- `contrato alvo`
- `legado temporario`
- `nao sera adotado`

## Temas Para Resposta

1. formato canonico de permissoes
2. status oficial da `permissions-matrix`
3. contrato canonico do stream
4. identificador canonico de visita
5. nomenclatura final de encomendas
6. publicacao estavel de `faceStatus`
7. contrato oficial de alertas e cameras

## Tensoes Ja Confirmadas Pelos Fronts

### Permissoes

Ainda aparecem formatos mistos como:

- `people.view`
- `alerts.view`
- `deliveries:create`
- `cameras:read`

### Stream

Ainda aparece dupla leitura entre:

- `type` x `eventType`
- `timestamp` x `occurredAt`

## Documento Base

Arquivo principal:

- `DECISOES_PENDENTES_DO_ECOSSISTEMA_2026-04-12.md`

Arquivos de apoio:

- `PENDENCIAS_BACKEND_ECOSSISTEMA_2026-04-12.md`
- `RELATORIO_DIVERGENCIAS_DES_RAFIELS_2026-04-12.md`

## Observacao Final

Se o `Backend` preferir, a resposta pode ser publicada em novo documento na `DES-RAFIELS` com prefixo:

- `BACKEND_`

O ideal e que a resposta seja objetiva, com decisao por item, para que os tres fronts possam convergir sem manter fallback desnecessario.
